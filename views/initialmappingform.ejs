<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width">
  <title>replit</title>
  <link href="style.css" rel="stylesheet" type="text/css" />
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>

<body>
  <h1>Key-Value Pair Form</h1>
  <form action="https://potential-pancake-w6w6qwvpqq729v5p-3000.app.github.dev/submit" method="POST">
    <!-- Loop through the array and generate options for each select -->
    <label for="address">address</label>
    <select id="address" name="address">
      <% propertiesname.forEach(function(property) { %>
        <option value="<%= property %>"><%= property %></option>
      <% }); %>
    </select><br>

    <label for="company">company:</label>
    <select id="company" name="company">
      <% propertiesname.forEach(function(property) { %>
        <option value="<%= property %>"><%= property %></option>
      <% }); %>
    </select><br>

    <label for="email">email:</label>
    <select id="email" name="email">
      <% propertiesname.forEach(function(property) { %>
        <option value="<%= property %>"><%= property %></option>
      <% }); %>
    </select><br>

    <label for="monthlybill">monthlybill:</label>
    <select id="monthlybill" name="monthlybill">
      <% propertiesname.forEach(function(property) { %>
        <option value="<%= property %>"><%= property %></option>
      <% }); %>
    </select><br>

    <label for="name">name:</label>
    <select id="name" name="name">
      <% propertiesname.forEach(function(property) { %>
        <option value="<%= property %>"><%= property %></option>
      <% }); %>
    </select><br>

    <button type="submit">submit</button>
  </form>

  <script></script>
</body>

</html>